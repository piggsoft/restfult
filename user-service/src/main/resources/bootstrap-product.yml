spring:
  application:
    name: user-mapper
  cloud:
    config:
      #profile: dev
      label: master
      discovery:
        enabled: true
        service-id: config-server
eureka:
  client:
    serviceUrl:
      defaultZone: http://piggsoft.mapper:18761/eureka/